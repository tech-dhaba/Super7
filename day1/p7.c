/*Implement a function pointer that takes a variable number of arguments and calculates their average. Use macros to handle the variable arguments.*/

#include<stdio.h>

#define average(...) FindAvg(...)
ZZ
