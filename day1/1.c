/*#include <stdio.h>

char rev(str *p,int len );
{
for(str *p=0 ; str *p<len ; str *p++)
	return(str*p);
}
int main() {
	printf("Enter a string : ");
	scanf("%d",str*p);
	rev(str*p);
	return 0;
}*/
#include <stdio.h>
char str(str);
 {
char str[100];
printf("Enter a string : ");
scanf("%s",str);
printf("You entered : %s",str);
return 0;
}


